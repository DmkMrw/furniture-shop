/* selectors */
export const getAllBrandsPics = ({ brands }) => brands;

export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
